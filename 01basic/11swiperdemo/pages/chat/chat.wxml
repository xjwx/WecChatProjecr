<view class="listview">
  <view class="itemview">
    <movable-area class="movablearea" style="width:{{(windowWidth-100)*2}}rpx">
      <movable-view class="movableview" style="width:{{(windowWidth)*2}}rpx" direction="horizontal"
       bindtouchstart="onTouchStateEvent" bindtouchend="onTouchEndEvent" bindchange="onChangeEvent"
       x="{{x}}">
        周杰伦
      </movable-view>
    </movable-area>
    <view class="deleteview">
      删除
    </view>
  </view>
</view>